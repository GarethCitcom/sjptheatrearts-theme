# Dependencies (dev tooling only -- the theme itself ships no build artefacts)
node_modules/
/vendor/

# Phase-review screenshots
/build/

# OS / editor
.DS_Store
Thumbs.db

# Archives
*.zip

# Note: acf-json/ is deliberately NOT ignored -- ACF field groups version in git.
